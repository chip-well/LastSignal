#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# Opt-in because some Brakeman releases can break this self-check transiently.
ARGV.unshift("--ensure-latest") if ENV["BRAKEMAN_ENSURE_LATEST"] == "1"

load Gem.bin_path("brakeman", "brakeman")
